# 2026-03-06 — Log-mel baseline: v10 → v20 summary

Summary
- Implemented representation-fair baseline `baseline_logmel_cnn_v10.ipynb` (fixed Conv1 local kernels, BatchNorm, GAP, callbacks).
- Implemented Strategy 1 variant `baseline_logmel_cnn_v20.ipynb` (deterministic 10s center-crop/pad; separate cache).

Why
- v1 used a Conv1 kernel that spanned the full mel axis, harming local-frequency learning. v10 fixes the kernel design and training pipeline.
- v20 enforces fixed-duration inputs (10s) so models see consistent frame counts and caches are shape-consistent.

Key artifact locations
- Notebooks:
  - MelCNN-MGR/notebooks/baseline_logmel_cnn_v10.ipynb
  - MelCNN-MGR/notebooks/baseline_logmel_cnn_v20.ipynb
- Cache roots (per-notebook):
  - v10 per-track `.npy`: `CACHE_DIR / "logmel" / ("shared" if LOGMEL_CACHE_SHARED else SUBSET)`
    - index parquet: `CACHE_DIR / f"logmel_index_{split_name}_{SUBSET}.parquet"`
    - individual files: `<LOGMEL_CACHE_DIR>/<track_id>.npy`
  - v20 per-track `.npy`: `CACHE_DIR / "logmel_10s" / ("shared" if LOGMEL_CACHE_SHARED else SUBSET)`
    - index parquet: `CACHE_DIR / f"logmel_10s_index_{split_name}_{SUBSET}.parquet"`
    - individual files: `<LOGMEL_CACHE_DIR>/<track_id>.npy`

Implementation notes
- v10 uses 30s clips (N_FRAMES ≈ 2582 with current hop settings).
- v20 uses CLIP_DURATION=10.0 → N_FRAMES ≈ 861. The pipeline loads audio, applies `normalize_to_fixed_duration()` (center-crop or pad), then computes mel spectrogram and saves with `np.save()` inside `_process_one_track()`.
- Cache separation (`logmel` vs `logmel_10s`) intentionally avoids collisions between different-duration features.

Next steps / checklist
- [ ] Verify on-disk caches and file counts under both cache roots.
- [ ] Run a quick end-to-end notebook execution for v10 and v20 (small subset) to validate shapes and training loop.
- [ ] Add short changelog markdown cell to `baseline_logmel_cnn_v10.ipynb` if missing (partial insert was previously attempted).

Notes
- Script `python /tmp/rebuild_logmel_notebook.py` was run successfully (Exit Code 0) when rebuilding notebooks in prior session.
- If you want, I can list the cache directories and show counts of `.npy` files now.

—
Generated by pair-programming assistant during March 6, 2026 session.
